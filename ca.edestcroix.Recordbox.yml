app-id: ca.edestcroix.Recordbox
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: recordbox
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
  - --socket=wayland
  - --filesystem=home
  - --socket=pulseaudio
  - --env=RUST_LOG=recordbox=warn
cleanup:
  - /include
  - /lib/pkgconfig
  - /man
  - /share/doc
  - /share/gtk-doc
  - /share/man
  - /share/pkgconfig
  - '*.la'
  - '*.a'
modules:
  - name: glycin-loaders
    buildsystem: meson
    config-opts:
      - -Dlibglycin=false
      - -Dtests=false
    sources:
      - type: archive
        url: https://download.gnome.org/sources/glycin/2.0/glycin-2.0.1.tar.xz
        sha256: f2a37ec85bb455af1e423b366de1c93e8f4f0deaa3c4c62c75c7528c6989eeb0
        x-checker-data:
          type: gnome
          name: glycin
  - name: blueprint-compiler
    buildsystem: meson
    cleanup:
      - '*'
    sources:
      - type: git
        url: https://gitlab.gnome.org/jwestman/blueprint-compiler
        tag: v0.16.0
  - name: recordbox
    builddir: true
    buildsystem: meson
    config-opts:
      - -Dbuildtype=release
    sources:
      - type: git
        url: https://codeberg.org/edestcroix/Recordbox/
        tag: v0.10.4
      - type: inline
        contents: |
          [source.vendored-sources]
          directory = "cargo/vendor"

          [source.crates-io]
          replace-with = "vendored-sources"
        dest: .cargo
        dest-filename: config.toml
      - cargo-sources.json
